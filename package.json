{
  "name": "draw-together",
  "version": "1.0.0",
  "description": "A collaborative drawing app where multiple people on different devices can draw on the same canvas at the same time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "hbs": "^4.0.1",
    "nodemon": "^1.11.0",
    "socket.io": "^2.0.3"
  }
}
